package stepDefinitions;

import org.junit.Assert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class LoginWithMultipleData
{
	
	/*
	 * WebDriver driver;
	 * 
	 * @When("Login page is displayed provide username as {string}") public void
	 * login_page_is_displayed_provide_username_as(String Username) {
	 * driver.findElement(By.id("user-name")).sendKeys(Username); }
	 * 
	 * @When("provide password as {string}") public void provide_password_as(String
	 * Password) { driver.findElement(By.id("password")).sendKeys(Password); }
	 * 
	 * @When("click on the login button") public void click_on_the_login_button() {
	 * driver.findElement(By.id("login-button")).click(); }
	 * 
	 * @Then("I validate for the Home Page") public void
	 * i_validate_for_the_home_page() { String actualURL = driver.getCurrentUrl();
	 * Assert.assertTrue(actualURL.contains("inventory"));
	 * System.out.println("Success"); }
	 */
	 
}
